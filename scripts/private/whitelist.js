class Whitelist extends Storage { // eslint-disable-line no-unused-vars
  constructor () {
    super({
      whitelist: {
        hosts: []
      }
    })
  }

  add (host) {
    
  }

  async query (host) {}
}
